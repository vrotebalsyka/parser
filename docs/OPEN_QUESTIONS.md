# Open Questions

1. Which exact official Form 4 sample should be treated as canonical for the production rule set?
2. What are the organization-specific wording requirements for candidate correction messages?
3. What file size/page count limits should be used in production policy?
4. Should the installer default to per-user install or Program Files with elevation?
5. What code-signing certificate and timestamp service will be used?
6. Should technical JSON be completely hidden or exposed through an advanced setting?
7. Should generated reports include source text snippets by default or only issue summaries?
8. Which PDF extractor dependency is approved by the organization?
9. Should release builds use a private NuGet feed/cache with package lock enforcement?
10. What is the official retention policy for generated reports outside the application?

